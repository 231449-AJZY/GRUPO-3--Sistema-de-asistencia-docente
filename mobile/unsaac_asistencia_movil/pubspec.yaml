name: unsaac_asistencia_movil
description: Aplicación móvil institucional para asistencia docente UNSAAC.
publish_to: "none"
version: 0.10.0+24
environment:
  sdk: ">=3.12.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  http: ^1.6.0
  local_auth: ^3.0.2
  mobile_scanner: ^7.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/certificates/unsaac_local_root_ca.pem
